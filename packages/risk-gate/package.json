{
  "name": "@fx/risk-gate",
  "version": "0.0.1",
  "private": true,
  "description": "BE-070/071 — deterministic risk-gate rule engine (final authority; never delegates to an LLM)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@fx/types": "workspace:*"
  },
  "devDependencies": {
    "@fx/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "vitest": "^4.1.9"
  }
}
