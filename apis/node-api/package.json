{
  "name": "@fx/node-api",
  "version": "0.0.1",
  "private": true,
  "description": "Fastify modular monolith — Phase 1 bootstrap (BE-001…003)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsup src/server.ts --format esm --clean",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@fx/types": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@fx/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.22.5",
    "vitest": "^4.1.9"
  }
}
