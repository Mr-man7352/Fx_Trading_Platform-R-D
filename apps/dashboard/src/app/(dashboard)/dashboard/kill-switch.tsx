/**
 * Superseded by the shared, API-wired `@/components/kill-switch` (FE-040). Kept
 * as a re-export so any lingering import path keeps resolving.
 */
export { KillSwitch } from '@/components/kill-switch';
