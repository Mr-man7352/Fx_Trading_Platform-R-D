{
  "name": "@fx/quant",
  "version": "0.0.1",
  "private": true,
  "description": "Quant service — FastAPI :5001 + gRPC :50051 (QN-001). Python deps are uv-managed, never pnpm. package.json exists only so `pnpm dev` (turbo) can boot it; requires uv (auto-provisions Python 3.13). Lint/type/test run via uv in the CI quant job, not turbo.",
  "scripts": {
    "dev": "uv run python -m app"
  }
}
